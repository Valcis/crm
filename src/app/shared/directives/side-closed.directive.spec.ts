import { SideClosedDirective } from './side-closed.directive';

describe('SideClosedDirective', () => {
  it('should create an instance', () => {
    const directive = new SideClosedDirective();
    expect(directive).toBeTruthy();
  });
});
